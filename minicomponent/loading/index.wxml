<view class="loading-content custom-class inline loading-{{size}}">
	 <view wx:if="{{type === 'circle'}}" class="loading-type circle"></view>
	 <view wx:if="{{type === 'circular'}}" class="loading-type circular"></view>
</view> 